from setuptools import setup, find_packages

setup(
    name='rva',
    version='0.1.0',
    description='A description of the rva package',
    author='Not Me',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here, for example:
        # 'numpy',
        # 'pandas',
    ],
    classifiers=[
        'Development Status :: 3 - Alpha',  # Change to Beta or Production/Stable as needed
    ]
)
